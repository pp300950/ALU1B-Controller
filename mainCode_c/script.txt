# Script ตัวอย่างที่แก้ไขแล้ว
VAR N 101       # N = 5
VAR X 1         # X = 1
VAR ONE 1       # สร้างตัวแปรสำหรับเลข 1
LOOP:
PRINT X
ADD X ONE       # ใช้ตัวแปร ONE ในการบวก
CMP X N         # เปรียบเทียบ X กับ N
JLE LOOP        # ถ้า X <= N ให้กระโดดไปที่ LOOP
PRINT X         # พิมพ์ผลลัพธ์สุดท้าย
